# Development environment
NEXT_PUBLIC_API_URL=http://localhost:8686

# Production builds bundled into the Go backend can leave NEXT_PUBLIC_API_URL empty
# so the frontend uses same-origin /api requests.
